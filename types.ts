// Define your TypeScript types and interfaces here

// Example:
export interface Product {
  id: string;
  name: string;
  description: string;
  price: number;
  images: string[];
  brand: string;
  isSoldOut: boolean;
  // Add more fields as needed
}